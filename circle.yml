machine:
  java:
    version: oraclejdk8

dependencies:
  pre:
    - chmod +x gradlew
    - echo y | android update sdk --no-ui --all --filter "platform-tools,tools,android-23,extra-google-m2repository,extra-android-m2repository,extra-android-support,sys-img-armeabi-v7a-android-22"
    - echo y | android update sdk --no-ui --all --filter "build-tools-23.0.3"

test:
  override:
    # start the emulator
    - emulator -avd circleci-android22 -no-audio -no-window:
        background: true
        parallel: true
    # wait for it to have booted
    - circle-android wait-for-boot
    - sh script.sh
